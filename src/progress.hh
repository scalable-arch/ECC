#include <stdio.h>
#include <stdlib.h>


const char bar = '=';
const char blank = ' ';

const int LEN = 20;
const int MAX = 10000;

void ProgressStatus(unsigned long long count, unsigned long long max_count);